{{- if and .Values.tls.enabled .Values.tls.certManager.enabled }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "opensearch.fullname" . }}-tls
  labels:
    {{- include "opensearch.labels" . | nindent 4 }}
spec:
  {{- with .Values.tls.certManager.certificate }}
  secretName: {{ .secretName | default (printf "%s-tls" (include "opensearch.fullname" $)) }}
  {{- if .commonName }}
  commonName: {{ .commonName }}
  {{- else }}
  commonName: {{ include "opensearch.fullname" $ }}.{{ $.Release.Namespace }}.svc.cluster.local
  {{- end }}
  dnsNames:
    {{- if .dnsNames }}
    {{- toYaml .dnsNames | nindent 4 }}
    {{- else }}
    # Default DNS names for the StatefulSet pods
    - {{ include "opensearch.fullname" $ }}.{{ $.Release.Namespace }}.svc.cluster.local
    - "*.{{ include "opensearch.fullname" $ }}.{{ $.Release.Namespace }}.svc.cluster.local"
    - "{{ template "opensearch.masterService" $ }}.{{ $.Release.Namespace }}.svc.cluster.local"
    - "{{ template "opensearch.masterService" $ }}-headless.{{ $.Release.Namespace }}.svc.cluster.local"
    {{- range $i := until (int $.Values.replicaCount) }}
    - {{ include "opensearch.fullname" $ }}-{{ $i }}.{{ include "opensearch.fullname" $ }}.{{ $.Release.Namespace }}.svc.cluster.local
    {{- end }}
    {{- end }}
  {{- if .ipAddresses }}
  ipAddresses:
    {{- toYaml .ipAddresses | nindent 4 }}
  {{- end }}
  duration: {{ .duration | default "2160h" }}
  renewBefore: {{ .renewBefore | default "360h" }}
  {{- end }}
  issuerRef:
    {{- if .Values.tls.certManager.issuer.name }}
    name: {{ .Values.tls.certManager.issuer.name }}
    {{- else }}
    name: {{ include "opensearch.fullname" . }}-issuer
    {{- end }}
    kind: {{ .Values.tls.certManager.issuer.kind | default "ClusterIssuer" }}
  # Key specifications
  privateKey:
    algorithm: RSA
    encoding: PKCS1
    size: 2048
  usages:
    - digital signature
    - key encipherment
    - server auth
    - client auth
  # Additional options for Java keystore generation
  keystores:
    jks:
      create: true
      passwordSecretRef:
        name: {{ include "opensearch.fullname" . }}-keystore-password
        key: password
    pkcs12:
      create: true
      passwordSecretRef:
        name: {{ include "opensearch.fullname" . }}-keystore-password
        key: password
{{- end }}
