# Trivy ignore file - suppress known upstream vulnerabilities
# These should be reviewed periodically and removed when fixed upstream

# OS - expat (base UBI image)
CVE-2025-59375

# Java - AxonOps Cassandra Agent (agent-c4-2.3.2-all.jar)
# Requires AxonOps team to update dependencies
CVE-2024-53990  # async-http-client (CRITICAL)
CVE-2025-52999  # jackson-core
CVE-2024-7254   # protobuf-java
CVE-2024-47554  # commons-io
CVE-2024-36114  # aircompressor
CVE-2022-1471   # snakeyaml
CVE-2022-25857  # snakeyaml (DoS - nested depth limitation)

# Java - K8ssandra Management API (upstream)
CVE-2025-24970  # netty-handler
CVE-2023-6378   # logback
CVE-2023-6481   # logback
CVE-2025-12183  # lz4-java 1.8.0 → 1.8.1 (HIGH - DoS risk, fixed in upstream)

# Java - Cassandra (fixed in later Cassandra versions)
CVE-2022-42003  # jackson-databind (fixed in Cassandra 5.0.3+, 4.1.8+, 4.0.16+)
CVE-2022-42004  # jackson-databind (fixed in Cassandra 5.0.3+, 4.1.8+)
CVE-2025-23015  # Cassandra user-defined functions (fixed in 5.0.3, 4.1.8, 4.0.16, 3.11.18, 3.0.31)

# Go - AxonOps binaries (axon-agent, axon-cassandra-restore, etc.)
# Requires rebuild with Go 1.24.11+ / 1.25.5+
CVE-2025-58183  # archive/tar
CVE-2025-58186  # HTTP headers
CVE-2025-58187  # name constraint
CVE-2025-61729  # stdlib HostnameError (HIGH - fixed in Go 1.24.11, 1.25.5)
