name: Cassandra Publish Signed to GHCR

# Publishes ghcr.io/axonops/cassandra/cassandra - the k8ssandra Dockerfile built
# with INCLUDE_MGMT_API=false, so the image carries Apache Cassandra and the
# AxonOps agent without the K8ssandra Management API.

on:
  workflow_dispatch:
    inputs:
      main_git_tag:
        description: 'Git tag on main branch to checkout and build (e.g., cassandra-1.0.0)'
        required: true
        type: string
      container_version:
        description: 'Build tag for GHCR - the third component of the image tag (e.g., 1.0.0)'
        required: true
        type: string
      axon_agent_version:
        description: 'AxonOps Agent version to install in the image (e.g., 2.0.31 or "latest")'
        required: false
        default: 'latest'
        type: string
      axon_agent_java_version:
        description: 'AxonOps Agent Java version to install (e.g., "latest" or "1.0.14")'
        required: false
        default: 'latest'
        type: string

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: axonops/cassandra/cassandra

jobs:
  # Version lists come from versions.yaml only. This image is built from the
  # same k8ssandra Dockerfiles and base digests as ghcr.io/axonops/k8ssandra/
  # cassandra, so both share one list and cannot drift apart.
  cassandra-versions:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.matrix.outputs.result }}
      all_versions: ${{ steps.all.outputs.result }}
      newest: ${{ steps.newest.outputs.result }}
    steps:
      - name: Checkout code at tag
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.main_git_tag }}

      - name: Validate build matrix against available k8ssandra base images
        run: ./scripts/build-matrix.sh check --k8ssandra-versions '${{ vars.K8SSANDRA_VERSIONS }}'

      - name: Build matrix
        id: matrix
        run: ./scripts/build-matrix.sh versions

      - name: All versions
        id: all
        run: ./scripts/build-matrix.sh space-separated

      - name: Newest version
        id: newest
        run: ./scripts/build-matrix.sh newest

  cassandra-validate:
    needs: cassandra-versions
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout code at tag
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.main_git_tag }}

      - name: Verify tag is on main branch
        run: |
          TAG_COMMIT=$(git rev-parse "${{ inputs.main_git_tag }}")
          git fetch origin main
          if git merge-base --is-ancestor "$TAG_COMMIT" origin/main; then
            echo "Tag ${{ inputs.main_git_tag }} is on main branch"
          else
            echo "ERROR: Tag ${{ inputs.main_git_tag }} is not on main branch"
            exit 1
          fi

      - name: Verify every matrix version has a k8ssandra base digest
        run: |
          for VERSION in ${{ needs.cassandra-versions.outputs.all_versions }}; do
            COMPOSITE_KEY=$(echo '${{ vars.K8SSANDRA_VERSIONS }}' | jq -r --arg v "$VERSION" 'keys[] | select(startswith($v + "+"))')
            if [ -z "$COMPOSITE_KEY" ]; then
              echo "ERROR: No k8ssandra base image found for Cassandra $VERSION"
              exit 1
            fi
          done
          echo "All matrix versions have a base image digest"

  cassandra-test:
    needs: [cassandra-versions, cassandra-validate]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      axon_agent_version: ${{ steps.verify.outputs.axon_agent_version }}
    steps:
      - name: Checkout code at tag
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.main_git_tag }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Parse k8ssandra base digest
        id: k8ssandra
        env:
          CASSANDRA_VERSION: ${{ needs.cassandra-versions.outputs.newest }}
        run: |
          COMPOSITE_KEY=$(echo '${{ vars.K8SSANDRA_VERSIONS }}' | jq -r --arg v "$CASSANDRA_VERSION" 'keys[] | select(startswith($v + "+"))')
          K8S_API_VERSION=$(echo "$COMPOSITE_KEY" | cut -d+ -f2)
          DIGEST=$(echo '${{ vars.K8SSANDRA_VERSIONS }}' | jq -r '.["'"$COMPOSITE_KEY"'"]')
          {
            echo "k8ssandra_api_version=${K8S_API_VERSION}"
            echo "digest=${DIGEST}"
            echo "base_version=$(echo "$CASSANDRA_VERSION" | cut -d. -f1,2)"
          } >> "$GITHUB_OUTPUT"

      - name: Build image for testing
        uses: docker/build-push-action@v5
        with:
          context: ./k8ssandra/${{ steps.k8ssandra.outputs.base_version }}
          file: ./k8ssandra/${{ steps.k8ssandra.outputs.base_version }}/Dockerfile
          platforms: linux/amd64
          load: true
          tags: axonops-cassandra:test
          build-args: |
            CASSANDRA_VERSION=${{ needs.cassandra-versions.outputs.newest }}
            MAJOR_VERSION=${{ steps.k8ssandra.outputs.base_version }}
            K8SSANDRA_BASE_DIGEST=${{ steps.k8ssandra.outputs.digest }}
            K8SSANDRA_API_VERSION=${{ steps.k8ssandra.outputs.k8ssandra_api_version }}
            INCLUDE_MGMT_API=false
            BUILD_DATE=${{ github.event.repository.updated_at }}
            VCS_REF=${{ github.sha }}
            VERSION=${{ inputs.container_version }}
            GIT_TAG=${{ inputs.main_git_tag }}
            GITHUB_ACTOR=${{ github.actor }}
            CQLAI_VERSION=${{ vars.K8SSANDRA_CQLAI_VERSION }}
            IS_PRODUCTION_RELEASE=true
            IMAGE_FULL_NAME=ghcr.io/${{ env.IMAGE_NAME }}:${{ needs.cassandra-versions.outputs.newest }}-test
            AXON_AGENT_VERSION=${{ inputs.axon_agent_version }}
            AXON_AGENT_JAVA_VERSION=${{ inputs.axon_agent_java_version }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Start container and wait for services
        uses: ./.github/actions/cassandra-start-and-wait
        with:
          container_name: cassandra-test

      - name: Verify versions and resolve the agent version
        id: verify
        uses: ./.github/actions/cassandra-verify-versions
        with:
          cassandra_version: ${{ needs.cassandra-versions.outputs.newest }}
          container_name: cassandra-test
          expected_agent_version: ${{ inputs.axon_agent_version }}

      - name: Verify AxonOps
        uses: ./.github/actions/k8ssandra-verify-axonops
        with:
          container_name: cassandra-test

      - name: Test cqlai
        uses: ./.github/actions/k8ssandra-test-cqlai
        with:
          container_name: cassandra-test

      - name: Run Trivy container security scan
        continue-on-error: true
        uses: aquasecurity/trivy-action@0.35.0
        with:
          image-ref: 'axonops-cassandra:test'
          format: 'table'
          exit-code: '1'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'
          trivyignores: 'k8ssandra/.trivyignore'

      - name: Cleanup
        if: always()
        run: |
          docker logs cassandra-test || true
          docker stop cassandra-test || true
          docker rm cassandra-test || true

  cassandra-check-tags:
    needs: [cassandra-versions, cassandra-test]
    runs-on: ubuntu-latest
    permissions:
      packages: read
      contents: read
    steps:
      - name: Check the immutable tags are not already published
        env:
          GH_TOKEN: ${{ github.token }}
          AGENT_VERSION: ${{ needs.cassandra-test.outputs.axon_agent_version }}
        run: |
          for VERSION in ${{ needs.cassandra-versions.outputs.all_versions }}; do
            TAG="${VERSION}-${AGENT_VERSION}-${{ inputs.container_version }}"
            if gh api --paginate "/orgs/axonops/packages/container/cassandra%2Fcassandra/versions" \
              --jq ".[] | select(.metadata.container.tags[] == \"$TAG\") | .metadata.container.tags[]" 2>/dev/null | grep -q "$TAG"; then
              echo "ERROR: Container tag $TAG already exists"
              exit 1
            fi
          done
          echo "All tags for build ${{ inputs.container_version }} are available"

  cassandra-create-release:
    needs: [cassandra-versions, cassandra-test, cassandra-check-tags]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code at tag
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.main_git_tag }}

      - name: Create GitHub Release
        uses: actions/github-script@v7
        env:
          AGENT_VERSION: ${{ needs.cassandra-test.outputs.axon_agent_version }}
        with:
          script: |
            const agent = process.env.AGENT_VERSION;
            const build = '${{ inputs.container_version }}';
            const versions = '${{ needs.cassandra-versions.outputs.all_versions }}'.split(' ');
            const latest = versions[versions.length - 1];
            const example = `${latest}-${agent}-${build}`;
            const body = [
              `AxonOps Apache Cassandra signed container images, build ${build}.`,
              ``,
              `Built from git tag: ${{ inputs.main_git_tag }}`,
              ``,
              `**Image path:** ghcr.io/axonops/cassandra/cassandra`,
              `**AxonOps agent:** ${agent}`,
              `**Cassandra versions:** ${versions.join(', ')}`,
              `**Signing:** keyless signing with Sigstore Cosign`,
              ``,
              `Built from the same Dockerfile as the K8ssandra image, with`,
              `INCLUDE_MGMT_API=false: the K8ssandra Management API and CDC agent are`,
              `removed, and Cassandra is started directly. For K8ssandra Operator`,
              `deployments use ghcr.io/axonops/k8ssandra/cassandra instead.`,
              ``,
              `**Tagging strategy:**`,
              `- Immutable: \`{CASSANDRA}-{AGENT}-{BUILD}\` (e.g. \`${example}\`)`,
              `- Latest build for a Cassandra + agent pair: \`{CASSANDRA}-{AGENT}\``,
              `- Latest agent and build for a Cassandra version: \`{CASSANDRA}\``,
              `- Minor-latest: \`5.0-latest\` (points to ${latest})`,
              `- Global-latest: \`latest\` (points to ${latest})`,
              ``,
              `**Verify signatures:**`,
              ``,
              '```bash',
              `cosign verify \\`,
              `  --certificate-identity-regexp="https://github.com/axonops/axonops-containers" \\`,
              `  --certificate-oidc-issuer="https://token.actions.githubusercontent.com" \\`,
              `  ghcr.io/axonops/cassandra/cassandra:${example}`,
              '```',
            ].join('\n');

            const release = await github.rest.repos.createRelease({
              owner: context.repo.owner,
              repo: context.repo.repo,
              tag_name: '${{ inputs.main_git_tag }}',
              name: `cassandra-${build}`,
              body,
              draft: false,
              prerelease: false
            });
            console.log(`Release created: ${release.data.html_url}`);

  cassandra-build-push-sign:
    needs: [cassandra-versions, cassandra-test, cassandra-check-tags, cassandra-create-release]
    runs-on: ubuntu-latest
    strategy:
      # One version failing must not cancel the others: a cancelled publish job
      # leaves the release half-pushed and the floating tags inconsistent.
      fail-fast: false
      max-parallel: 3
      matrix:
        cassandra_version: ${{ fromJSON(needs.cassandra-versions.outputs.matrix) }}
    permissions:
      contents: read
      packages: write
      id-token: write
    env:
      AGENT_VERSION: ${{ needs.cassandra-test.outputs.axon_agent_version }}
    steps:
      - name: Checkout code at tag
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.main_git_tag }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Parse k8ssandra base digest
        id: k8ssandra
        env:
          CASSANDRA_VERSION: ${{ matrix.cassandra_version }}
        run: |
          COMPOSITE_KEY=$(echo '${{ vars.K8SSANDRA_VERSIONS }}' | jq -r --arg v "$CASSANDRA_VERSION" 'keys[] | select(startswith($v + "+"))')
          K8S_API_VERSION=$(echo "$COMPOSITE_KEY" | cut -d+ -f2)
          DIGEST=$(echo '${{ vars.K8SSANDRA_VERSIONS }}' | jq -r '.["'"$COMPOSITE_KEY"'"]')
          {
            echo "k8ssandra_api_version=${K8S_API_VERSION}"
            echo "digest=${DIGEST}"
          } >> "$GITHUB_OUTPUT"

      - name: Determine latest tags
        id: version
        uses: ./.github/actions/k8ssandra-determine-latest
        with:
          current_version: ${{ matrix.cassandra_version }}
          all_versions: ${{ needs.cassandra-versions.outputs.all_versions }}

      # Pushed by digest only: no tag exists until the image has passed its
      # tests and been signed, so a failure here leaves nothing pullable.
      - name: Build and push by digest (untagged)
        id: build
        uses: docker/build-push-action@v5
        with:
          context: ./k8ssandra/${{ steps.version.outputs.base_version }}
          file: ./k8ssandra/${{ steps.version.outputs.base_version }}/Dockerfile
          platforms: linux/amd64,linux/arm64
          outputs: type=image,name=ghcr.io/${{ env.IMAGE_NAME }},push-by-digest=true,name-canonical=true,push=true
          annotations: |
            index:org.opencontainers.image.created=${{ github.event.repository.updated_at }}
            index:org.opencontainers.image.authors=AxonOps
            index:org.opencontainers.image.url=https://axonops.com
            index:org.opencontainers.image.documentation=https://github.com/axonops/axonops-containers/blob/main/cassandra/README.md
            index:org.opencontainers.image.source=https://github.com/axonops/axonops-containers
            index:org.opencontainers.image.version=${{ inputs.container_version }}
            index:org.opencontainers.image.revision=${{ github.sha }}
            index:org.opencontainers.image.vendor=AxonOps
            index:org.opencontainers.image.licenses=Apache-2.0
            index:org.opencontainers.image.title=AxonOps Apache Cassandra ${{ matrix.cassandra_version }}
            index:org.opencontainers.image.description=Apache Cassandra ${{ matrix.cassandra_version }} with the AxonOps agent ${{ env.AGENT_VERSION }} and cqlai, without the K8ssandra Management API. Multi-arch (amd64/arm64), signed with cosign.
            index:org.opencontainers.image.ref.name=ghcr.io/${{ env.IMAGE_NAME }}:${{ matrix.cassandra_version }}-${{ env.AGENT_VERSION }}-${{ inputs.container_version }}
            index:org.opencontainers.image.base.digest=${{ steps.k8ssandra.outputs.digest }}
            index:com.axonops.cassandra.version=${{ matrix.cassandra_version }}
            index:com.axonops.axon-agent.version=${{ env.AGENT_VERSION }}
            index:com.axonops.k8ssandra-api.included=false
            index:com.axonops.build.actor=${{ github.actor }}
          build-args: |
            CASSANDRA_VERSION=${{ matrix.cassandra_version }}
            MAJOR_VERSION=${{ steps.version.outputs.base_version }}
            K8SSANDRA_BASE_DIGEST=${{ steps.k8ssandra.outputs.digest }}
            K8SSANDRA_API_VERSION=${{ steps.k8ssandra.outputs.k8ssandra_api_version }}
            INCLUDE_MGMT_API=false
            BUILD_DATE=${{ github.event.repository.updated_at }}
            VCS_REF=${{ github.sha }}
            VERSION=${{ inputs.container_version }}
            GIT_TAG=${{ inputs.main_git_tag }}
            GITHUB_ACTOR=${{ github.actor }}
            CQLAI_VERSION=${{ vars.K8SSANDRA_CQLAI_VERSION }}
            IS_PRODUCTION_RELEASE=true
            IMAGE_FULL_NAME=ghcr.io/${{ env.IMAGE_NAME }}:${{ matrix.cassandra_version }}-${{ env.AGENT_VERSION }}-${{ inputs.container_version }}
            AXON_AGENT_VERSION=${{ env.AGENT_VERSION }}
            AXON_AGENT_JAVA_VERSION=${{ inputs.axon_agent_java_version }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Pull the published image for testing
        run: |
          docker pull ghcr.io/${{ env.IMAGE_NAME }}@${{ steps.build.outputs.digest }}
          docker tag ghcr.io/${{ env.IMAGE_NAME }}@${{ steps.build.outputs.digest }} axonops-cassandra:published

      - name: Start container and wait for services
        uses: ./.github/actions/cassandra-start-and-wait
        with:
          container_name: cassandra-test-${{ matrix.cassandra_version }}
          image: axonops-cassandra:published

      - name: Verify versions
        uses: ./.github/actions/cassandra-verify-versions
        with:
          cassandra_version: ${{ matrix.cassandra_version }}
          container_name: cassandra-test-${{ matrix.cassandra_version }}
          expected_agent_version: ${{ env.AGENT_VERSION }}

      - name: Verify AxonOps
        uses: ./.github/actions/k8ssandra-verify-axonops
        with:
          container_name: cassandra-test-${{ matrix.cassandra_version }}

      - name: Test cqlai
        uses: ./.github/actions/k8ssandra-test-cqlai
        with:
          container_name: cassandra-test-${{ matrix.cassandra_version }}

      - name: Collect logs
        if: always()
        uses: ./.github/actions/k8ssandra-collect-logs
        with:
          container_name: cassandra-test-${{ matrix.cassandra_version }}
          cassandra_version: ${{ matrix.cassandra_version }}

      - name: Cleanup test container
        if: always()
        run: |
          docker stop cassandra-test-${{ matrix.cassandra_version }} || true
          docker rm cassandra-test-${{ matrix.cassandra_version }} || true

      - name: Sign container image
        uses: ./.github/actions/k8ssandra-sign-container
        with:
          image: ghcr.io/${{ env.IMAGE_NAME }}@${{ steps.build.outputs.digest }}

      # Tags are applied last, to an image that is already tested and signed.
      # imagetools create copies the index without rebuilding, so every tag
      # resolves to exactly the digest that was signed - asserted below.
      - name: Apply tags to the verified image
        env:
          IMAGE: ghcr.io/${{ env.IMAGE_NAME }}
          DIGEST: ${{ steps.build.outputs.digest }}
          TAG_IMMUTABLE: ${{ matrix.cassandra_version }}-${{ env.AGENT_VERSION }}-${{ inputs.container_version }}
          TAG_PAIR: ${{ matrix.cassandra_version }}-${{ env.AGENT_VERSION }}
          TAG_CASSANDRA: ${{ matrix.cassandra_version }}
          TAG_MINOR_LATEST: ${{ steps.version.outputs.is_minor_latest == 'true' && format('{0}-latest', steps.version.outputs.base_version) || '' }}
          TAG_GLOBAL_LATEST: ${{ steps.version.outputs.is_global_latest == 'true' && 'latest' || '' }}
        run: |
          TAGS=()
          for TAG in "$TAG_IMMUTABLE" "$TAG_PAIR" "$TAG_CASSANDRA" "$TAG_MINOR_LATEST" "$TAG_GLOBAL_LATEST"; do
            if [ -n "$TAG" ]; then TAGS+=("$TAG"); fi
          done

          ARGS=()
          for TAG in "${TAGS[@]}"; do ARGS+=(-t "${IMAGE}:${TAG}"); done
          docker buildx imagetools create "${ARGS[@]}" "${IMAGE}@${DIGEST}"

          for TAG in "${TAGS[@]}"; do
            RESOLVED=$(docker buildx imagetools inspect "${IMAGE}:${TAG}" --format '{{ .Manifest.Digest }}')
            if [ "$RESOLVED" != "$DIGEST" ]; then
              echo "ERROR: ${IMAGE}:${TAG} resolves to ${RESOLVED}, expected the signed digest ${DIGEST}"
              exit 1
            fi
            echo "${IMAGE}:${TAG} -> ${DIGEST}"
          done

      - name: Image published and signed
        run: |
          echo "Published and signed:"
          echo "  ghcr.io/${{ env.IMAGE_NAME }}:${{ matrix.cassandra_version }}-${{ env.AGENT_VERSION }}-${{ inputs.container_version }}"
          echo "  digest ${{ steps.build.outputs.digest }}"
