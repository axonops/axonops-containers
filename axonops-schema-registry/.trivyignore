# AxonOps Schema Registry Trivy Ignore List
# CVEs that are known, accepted, or have no fix available
# Review periodically and remove when fixed upstream
#
# Format: CVE-YYYY-NNNNN  # Component: reason for ignoring

# Go stdlib v1.25.5 — upstream binary built with older Go version
# These will be resolved when upstream releases a new version built with Go >= 1.25.7
CVE-2025-68121  # Go stdlib crypto/tls: unexpected session resumption (CRITICAL - upstream Go v1.25.5, fixed in 1.25.7)
CVE-2025-61726  # Go stdlib net/url: memory exhaustion in query parameter parsing (HIGH - upstream Go v1.25.5, fixed in 1.25.6)
CVE-2025-61728  # Go stdlib archive/zip: excessive CPU consumption building archive index (HIGH - upstream Go v1.25.5, fixed in 1.25.6)
CVE-2025-61730  # Go stdlib crypto/tls: TLS 1.3 handshake issue with multiple messages (HIGH - upstream Go v1.25.5, fixed in 1.25.6)
