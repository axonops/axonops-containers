apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "axon-cassandra.fullname" . }}-test-connection"
  labels:
    {{- include "axon-cassandra.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "axon-cassandra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
