apiVersion: v1
kind: Service
metadata:
  name: {{ include "axon-dash.fullname" . }}-svc
  labels:
    {{- include "axon-dash.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "axon-dash.selectorLabels" . | nindent 4 }}
